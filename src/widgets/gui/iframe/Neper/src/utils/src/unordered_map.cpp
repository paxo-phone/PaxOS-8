#include <unordered_map> 

#include "../include/unordered_map.hpp"

// template<typename T1, typename T2>
// bool contains (T1 key, const std::unordered_map<T1, T2>& map) 
// {
//     return !(map.find(key) == map.end()); 
// }

// bool contains(std::string key, const std::unordered_map<std::string, abstract::color_t>& map)
// {
//     return !(map.find(key) == map.end()); 
// }
